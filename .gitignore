# Build output — giai đoạn 2
bin/
obj/
dist/
node_modules/
.angular/
*.user
.vs/

# Môi trường
.env
.env.local
appsettings.Local.json
# Chứng chỉ HTTPS dev xuất cho Angular dev server — không bao giờ commit
src/FE/.cert/

# Hệ điều hành
Thumbs.db
.DS_Store

# Dau trang thai cua hook — cuc bo, khong commit
.claude/.state/

# Bản xem thử HTML sinh từ docs/Design/ — dùng một lần, không phải nguồn (CLAUDE.md §7), không commit
preview/
